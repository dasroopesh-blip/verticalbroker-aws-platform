# VerticalBroker Platform - Lambda Layer Dependencies
# These are packaged into Lambda layers for deployment
# Does NOT include boto3/botocore (provided by Lambda runtime)
# Build: pip install -r requirements/lambda-layer.txt -t python/lib/python3.12/site-packages/

aws-lambda-powertools[all]>=2.30.0,<3.0.0
pyarrow>=14.0.0,<16.0.0
pydantic>=2.5.0,<3.0.0
python-dateutil>=2.8.2,<3.0.0
